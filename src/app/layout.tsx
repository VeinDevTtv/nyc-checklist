import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "NYC Trip Checklist - Your Ultimate Guide to New York City",
  description: "A comprehensive and interactive checklist for exploring New York City. Discover must-see attractions, world-class museums, diverse neighborhoods, iconic foods, and essential travel tips for the Big Apple.",
  keywords: ["NYC", "New York City", "travel", "checklist", "attractions", "museums", "food", "tourism"],
  authors: [{ name: "NYC Trip Checklist" }],
  creator: "NYC Trip Checklist",
  publisher: "NYC Trip Checklist",
  formatDetection: {
    email: false,
    address: false,
    telephone: false,
  },
  viewport: {
    width: "device-width",
    initialScale: 1,
    maximumScale: 1,
    userScalable: false,
  },
  themeColor: [
    { media: "(prefers-color-scheme: light)", color: "#667eea" },
    { media: "(prefers-color-scheme: dark)", color: "#1a1a2e" },
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased font-sans`}
      >
        {children}
      </body>
    </html>
  );
}
